# better-sqlite3 ships prebuilt binaries but declares no install script, so npm
# falls back to `node-gyp rebuild` and demands a C++ toolchain to rebuild what is
# already there. No dependency here needs its install script; CI runs this way too.
ignore-scripts=true
